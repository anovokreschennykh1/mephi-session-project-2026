    1  sudo hostnamectl set-hostname mephi-2026.domain.local
    2  hostnamectl status
    3  nmcli device status
    4  nmcli connection show
    5  sudo nmcli connection modify enp0s8 ipv4.addresses 192.168.1.100/24 ipv4.gateway 192.168.1.1 ipv4.dns 8.8.8.8 ipv4.method manual connection.autoconnect yes
    6  sudo nmcli connection up enp0s8
    7  ip route
    8  ping -c 4 8.8.8.8 > /tmp/network_check.txt
    9  ping -c 4 192.168.1.1 >> /tmp/network_check.txt
   10  cat /tmp/network_check.txt
   11  sudo dnf install -y nginx tcpdump libcap-ng-utils
   12  sudo dnf install -y dnf-plugins-core policycoreutils-python-utils firewalld openssh-server git tar
   13  dnf download tcpdump --destdir=/tmp
   14  ls -lh /tmp/tcpdump*.rpm
   15  sudo rpm -Uvh --replacepkgs /tmp/tcpdump*.rpm
   16  rpm -q tcpdump
   17  lsblk -f
   18  sudo fdisk /dev/sdb
   19  sudo mkfs.ext4 -F -L MEPHI_DATA /dev/sdb1
   20  sudo mkdir -p /data/mephi-web
   21  echo "LABEL=MEPHI_DATA /data/mephi-web ext4 defaults 0 2" | sudo tee -a /etc/fstab
   22  sudo mount -a
   23  df -hT | grep mephi-web
   24  sudo systemctl enable --now nginx
   25  systemctl status nginx --no-pager
   26  sudo journalctl -u nginx --since "5 minutes ago" > /tmp/nginx_recent_logs.txt
   27  cat /tmp/nginx_recent_logs.txt
   28  sudo groupadd mephi-devs
   29  sudo useradd -m -G mephi-devs mephi-admin
   30  echo 'mephi-admin:P@ssw0rd2026' | sudo chpasswd
   31  sudo chown mephi-admin:mephi-devs /data/mephi-web
   32  sudo chmod 2775 /data/mephi-web
   33  sudo usermod -aG wheel mephi-admin
   34  sudo systemctl enable --now sshd
   35  sudo systemctl enable --now firewalld
   36  sudo firewall-cmd --permanent --add-service=ssh
   37  sudo firewall-cmd --reload
   38  getenforce
   39  sudo semanage fcontext -a -t httpd_sys_content_t "/data/mephi-web(/.*)?"
   40  sudo restorecon -Rv /data/mephi-web
   41  ls -Zd /data/mephi-web
   42  sudo chmod u-s /usr/sbin/tcpdump
   43  sudo  setcap 'cap_net_raw,cap_net_admin=ep' /usr/sbin/tcpdump
   44  getcap /usr/sbin/tcpdump
   45  sudo -u mephi-admin tcpdump --help > /tmp/tcpdump_help.txt
   46  head /tmp/tcpdump_help.txt
   47  echo 'root' | sudo tee /etc/security/denied_users
   48  sudo chown root:root /etc/security/denied_users
   49  sudo chmod 600 /etc/security/denied_users
   50  PAM_RULE='auth required pam_listfile.so item=user sense=deny file=/etc/security/denied_users onerr=fail'
   51  grep -qF "$PAM_RULE" /etc/pam.d/login || sudo sed -i "1a $PAM_RULE" /etc/pam.d/login
   52  grep -qF "$PAM_RULE" /etc/pam.d/sshd || sudo sed -i "1a $PAM_RULE" /etc/pam.d/sshd
   53  sudo mkdir -p /etc/ssh/sshd_config.d
   54  echo 'PermitRootLogin no' | sudo tee /etc/ssh/sshd_config.d/99-disable-root-login.conf
   55  sudo systemctl restart sshd
   56  sudo -u mephi-admin bash -c 'echo "Hello from Student: 369046" > /data/mephi-web/index.html'
   57  sudo chmod 644 /data/mephi-web/index.html
   58  sudo restorecon -v /data/mephi-web/index.html
   59  sudo sed -i 's|root[[:space:]]\+/usr/share/nginx/html;| root /data/mephi-web;|g' /etc/nginx/nginx.conf
   60  sudo firewall-cmd --permanent --add-service=http
   61  sudo firewall-cmd --reload
   62  sudo nginx -t
   63  sudo systemctl restart nginx
   64  curl http://localhost
   65  curl http://192.168.1.100
   66  sudo mkdir -p /project_artifacts
   67  sudo chown mephi-admin:mephi-devs /project_artifacts
   68  sudo chmod 2775 /project_artifacts
   69  history > /project_artifacts/project_history.txt
